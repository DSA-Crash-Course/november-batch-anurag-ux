var a = 10;
var str = "hello";

var flags = true;

flag = !flag;

//      push
//add -> adds my files to the staging area
//commit -> bind all my files together into a single commit
//push -> add all our commits to the online repository(github)